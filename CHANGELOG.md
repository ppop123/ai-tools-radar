# 更新日志 · Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## v1.0.2 — 2026-09-02

数据快照 2026-09-02(`DATA_VERSION` 已同步)。新增博客评论库。

| 数据 | 规模 | 较 v1.0.1 |
|---|---|---|
| 收录站点 | 21,207 | +748 |
| 有流量或排名数据 | 12,996 | +417 |
| 有中英文描述 | 14,967 | +748 |
| 有近 12 个月流量曲线 | 4,463 | 持平 |
| 有渠道构成 | 5,225 | 持平 |
| 外链库页面 | 12,000 | 持平 |
| 单域 dofollow 明细 | 2,253 域 / 223,233 行 | 持平 |
| 博客评论库(新) | 377 条(复核日 live 324) | 新增 |

新增:`data/verified_blog_comments.{csv,json}` —— 377 个实测被 WP 评论接口受理的
博客评论地址(含文章页 URL),发布当日全量复核存活(live 324 / lost 22 / unknown 31,
live 中 dofollow 仅 6);配套用法手册 `docs/blog-comment-sites-playbook.md`。

---

## v1.0.1 — 2026-08-26

数据快照 2026-08-26(`DATA_VERSION` 已同步)。口径变化:总榜去掉「无流量数据不上榜」
阀门——名录抓取成功且 LLM 打标在案的站即可上榜,无流量/排名的排尾部;
分类器判非工具(is_tool=false)仍不上榜。

| 数据 | 规模 | 较 v1.0.0 |
|---|---|---|
| 收录站点 | 20,459 | +4,797 |
| 有流量或排名数据 | 12,579 | +1,772(当日补过一次 aitdk 流量富集) |
| 有中英文描述 | 14,219 | +2,667 |
| 有近 12 个月流量曲线 | 4,463 | 持平 |
| 有渠道构成 | 5,225 | 持平 |
| 外链库页面 | 12,000 | 持平 |
| 单域 dofollow 明细 | 2,253 域 / 223,233 行 | 持平(v1.0.0 文档写 1,360 域系文档滞后,8/18 数据已是 2,253 域) |

---

## v1.0.0 — 2026-08-18

首个正式版本。两块东西:按真实流量排名的 AI 工具站数据库,以及一条把"竞品外链来源"
变成投放清单并自动提交的管道。

### 数据站(`index.html` + `data/`)

单文件静态站,原生 JS,无框架无构建。四个视图:总榜 / 增长榜 / 新品雷达 / 外链库,
中英双语。

| 数据 | 规模 |
|---|---|
| 收录站点 | 15,662 |
| 有流量或排名数据 | 10,807 |
| 有中英文描述 | 11,552 |
| 有近 12 个月流量曲线 | 4,463 |
| 有渠道构成 | 5,225 |
| 外链库页面 | 12,000 |
| 单域 dofollow 明细 | 1,360 域 / 135,230 行(每域按权重分取 top 100) |

快照日期 2026-08-17(`index.html` 顶部的 `DATA_VERSION`,换数据时改它)。

### 外链投放管道(`outreach/`)

移植自一套生产验证过的私有系统。三条红线由代码硬执行,LLM 无权越过:付费即停 /
文案过 `forbidden_claims` 闸门 / 验证码不让 LLM 编答案。

完整闭环:`targets.py` 生成清单 → `driver.py` 滚动投放(LLM-in-the-loop 浏览器代理)
→ `mail_sweeper.py` 收验证信并点链接 → `verify_link.mjs` 终核确认链接真的上线。

**防重复投递有两道独立的闸**:账本状态迁移守卫 + `claims/<域>.claim` 的 O_EXCL
一次性标记。后者是内核保证的原子创建,不依赖任何存活检测或陈旧判断 ——
"会不会重复 POST"因此不依赖文件锁的正确性。

### 配置规范化(本版新增)

- `llm_config.py` / `.mjs` —— LLM 端点配置的唯一解析口,两份实现规则逐条一致
  (43 组用例 + 7 组共享常量 + 7 组重定向同源,跨语言对拍)
- **填 base URL 就行**,不用自己拼 `/chat/completions`;根域 / `/v1` / 带尾斜杠 /
  完整地址 / `/v1beta` / Azure 带 query 六种写法都归一
- 兼容通用的 `OPENAI_BASE_URL` / `OPENAI_API_KEY`,也支持 `llm.json` 配置文件
- **base 与 key 按来源单元整体绑定**,不按字段各自降级 —— 否则会把 A 供应商的
  key 发给 B 供应商的地址;配置有歧义时直接报错,不猜
- `configure.py` —— 本机配置界面(只绑 127.0.0.1、一次性 token、Host 环回校验、
  Origin 同源、key 只回掩码、写文件 0600)
- `check_llm.py` —— 端点自检(连通性 + `response_format: json_object`);
  `mail_sweeper` 启动时实探,不通就拒绝开跑

### 测试(本版新增)

`bash outreach/tests/smoke.sh` 一条命令跑完:语法 / Python 关键路径 29 项 /
Node 关键路径 47 项 / 配置对拍 43 组 / 12 进程并发认领。

**"语法过 + import 过"不算回归** —— 这个脚本存在的原因是:曾经整段替换函数时
连带删掉了 `state.py` 的 6 个函数,语法和 import 都照样通过,`NameError` 只在真正
调用到那一行时才炸。

### 工程记录

代码经过 8 轮独立外审(Codex)+ 多轮自审,累计修复 90+ 条问题,其中 P1 级 30+。
逐轮的 finding、修法与实测记录在 `docs/CODEX_REVIEW.md`。

外审 P1 数量的收敛过程:`5 → 6 → 7 → 5 → 5 → 3 → 0 → 0`。

### 已知限制

- **`outreach/` 尚未端到端真跑过**:所有验证都是读代码 + 定向复现 + 冒烟,
  没有用真 key 对真实站点跑完整流程。首次使用请 `driver.py --limit 5` 小批验证。
- `verify_link` 的判死门槛经过四层收紧,**没有真实域名样本验证过三态分布**,
  可能偏保守(真掉链的站攒不够 3 次 `offline_confirmed`)。
- 账本是 append-only 的 JSONL 且**没有压实路径**,所有读都是全文件扫描
  (实测 100 万行 / 316MB 时单次写耗时 4 秒)。`--loop` 长跑数月需要关注。
- `alerts` / `mail_ws` 两个模块不在本仓 —— 告警只进日志,收信延迟退化成周期轮询。
- `scripts/` 的构建脚本读私有数据湖,外部跑不了;路径是作者本机的绝对路径。
- 前端总榜一次渲染 15,662 行(`innerHTML` 建 DOM 约 327ms),搜索框已加 140ms 防抖,
  但**没有做分页/虚拟化**。
